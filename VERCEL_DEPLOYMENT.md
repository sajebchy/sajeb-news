# 🚀 Vercel Deployment Guide - Sajeb NEWS

এই গাইড অনুসরণ করে আপনি Sajeb NEWS প্রজেক্টটি Vercel এ ডিপ্লয় করতে পারবেন।

## ✅ প্রিরিকোয়ারমেন্ট

1. **Vercel অ্যাকাউন্ট** - https://vercel.com (বিনামূল্যে সাইন আপ করুন)
2. **GitHub অ্যাকাউন্ট** - প্রজেক্ট রিপোজিটরি থাকতে হবে
3. **Database** - MySQL হোস্ট (যেমন: Aiven, Planetscale, AWS RDS ইত্যাদি)

---

## 📋 Step 1: Vercel এ লগইন করুন

1. https://vercel.com যান
2. GitHub দিয়ে লগইন করুন
3. Authorize করুন

---

## 🔗 Step 2: প্রজেক্ট ইম্পোর্ট করুন

### Method 1: GitHub থেকে সরাসরি (সবচেয়ে সহজ)

1. **Vercel Dashboard** এ যান
2. **"Add New"** ক্লিক করুন → **"Project"**
3. **"Import Git Repository"** ক্লিক করুন
4. আপনার `sajeb-news` রিপোজিটরি সার্চ করুন এবং সিলেক্ট করুন
5. **"Import"** ক্লিক করুন

### Method 2: CLI ব্যবহার করে

```bash
npm i -g vercel
vercel
# গাইডেন্স অনুসরণ করুন
```

---

## ⚙️ Step 3: এনভায়রনমেন্ট ভেরিয়েবল কনফিগার করুন

Vercel Project Settings → **Environment Variables** এ যান এবং নিম্নলিখিত ভেরিয়েবল যোগ করুন:

```
APP_NAME=Sajeb NEWS
APP_ENV=production
APP_KEY=base64:YOUR_APP_KEY_HERE   (php artisan key:generate থেকে পান)
APP_DEBUG=false
LOG_CHANNEL=stack

DB_CONNECTION=mysql
DB_HOST=your_db_host.com
DB_PORT=3306
DB_DATABASE=sajeb_news
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password

CACHE_DRIVER=file
SESSION_DRIVER=cookie
QUEUE_CONNECTION=database

MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=your_mail_user
MAIL_PASSWORD=your_mail_password
MAIL_FROM_ADDRESS=noreply@sajebnews.com
MAIL_FROM_NAME="Sajeb NEWS"
```

**APP_KEY কীভাবে পাবেন:**
```bash
php artisan key:generate --show
```

---

## 🗄️ Step 4: Database সেটআপ করুন

আপনার Database Provider এ MySQL ডাটাবেস তৈরি করুন:

### সুপারিশকৃত ফ্রি ডাটাবেস সার্ভিস:
- **PlanetScale** (MySQL ফ্রি)
- **Aiven** (MySQL ফ্রি ট্রায়াল)
- **AWS RDS** (ফ্রি টায়ার)
- **Railway.app** (ফ্রি ক্রেডিট)

**ডাটাবেস নাম:** `sajeb_news`

---

## 🔑 Step 5: প্রজেক্ট সেটিংস

Vercel Dashboard → Project Settings → **Build & Development Settings**

নিশ্চিত করুন:
- **Framework Preset:** Laravel
- **Build Command:** `composer install --optimize-autoloader --no-dev && npm install && npm run build && php artisan migrate --force`
- **Install Command:** `composer install`
- **Output Directory:** `public`

---

## 🚀 Step 6: ডিপ্লয় করুন

সবকিছু করার পরে:

1. **"Deploy"** বাটন ক্লিক করুন
2. ডিপ্লয়মেন্ট সম্পূর্ণ হওয়ার জন্য অপেক্ষা করুন (5-10 মিনিট)
3. Vercel আপনার অ্যাপ URL প্রদান করবে

---

## ✨ ডিপ্লয়মেন্টের পরে

### Admin Panel অ্যাক্সেস করুন
```
https://your-project.vercel.app/admin
Email: admin@test.com
Password: 12345
```

### কাস্টম ডোমেইন যোগ করুন
1. Vercel → Project Settings → **Domains**
2. আপনার ডোমেইন যোগ করুন
3. DNS সেটিংস আপডেট করুন

---

## 🐛 সমস্যা সমাধান

### ত্রুটি: "Database Connection Failed"
- এনভায়রনমেন্ট ভেরিয়েবল পুনরায় চেক করুন
- Database সংযোগ কাজ করছে কিনা যাচাই করুন

### ত্রুটি: "View Not Found"
```bash
php artisan view:cache
git add .
git commit -m "cache views"
git push
```

### ত্রুটি: "Storage Permission Denied"
Vercel স্বয়ংক্রিয়ভাবে `/tmp` ডিরেক্টরি ব্যবহার করে storage এর জন্য।

### ত্রুটি: "Migration Failed"
```bash
# স্থানীয়ভাবে পরীক্ষা করুন:
php artisan migrate --force
```

---

## 📊 ডাটাবেস মাইগ্রেশন স্বয়ংক্রিয় করুন

প্রতিটি ডিপ্লয়মেন্টে স্বয়ংক্রিয়ভাবে মাইগ্রেশন চালাতে:

**vercel.json** এ ইতিমধ্যে সংযোজিত আছে:
```json
"buildCommand": "composer install --optimize-autoloader --no-dev && npm install && npm run build && php artisan migrate --force"
```

---

## 🔄 CI/CD পাইপলাইন

Vercel স্বয়ংক্রিয়ভাবে:
1. Git পুশ ডিটেক্ট করে
2. বিল্ড চালায়
3. নতুন সংস্করণ ডিপ্লয় করে
4. আপনাকে email পাঠায়

---

## 📈 পারফরম্যান্স অপটিমাইজেশন

### Vercel এ দ্রুত লোডিং এর জন্য:

1. **Static Files ক্যাশিং**
   - ইতিমধ্যে `public/.htaccess` এ কনফিগার করা আছে

2. **Environment ভেরিয়েবল অপটিমাইজ করুন**
   ```
   CACHE_DRIVER=file (বা redis)
   SESSION_DRIVER=cookie
   ```

3. **Database Optimization**
   - ইন্ডেক্স যোগ করুন
   - কোয়েরি অপটিমাইজ করুন

---

## 📞 সাপোর্ট এবং সম্পদ

- **Vercel Docs:** https://vercel.com/docs
- **Laravel Docs:** https://laravel.com/docs
- **Sajeb NEWS Repository:** https://github.com/sajebchy/sajeb-news

---

## ✅ চেকলিস্ট

সফল ডিপ্লয়মেন্টের জন্য:

- [ ] Vercel অ্যাকাউন্ট তৈরি করা হয়েছে
- [ ] প্রজেক্ট ইম্পোর্ট করা হয়েছে
- [ ] এনভায়রনমেন্ট ভেরিয়েবল যোগ করা হয়েছে
- [ ] Database সংযোগ কাজ করছে
- [ ] প্রথম ডিপ্লয়মেন্ট সফল হয়েছে
- [ ] Admin প্যানেল অ্যাক্সেসযোগ্য
- [ ] কাস্টম ডোমেইন যোগ করা হয়েছে (ঐচ্ছিক)

---

## 🎉 উদযাপন করুন!

আপনার প্রজেক্ট এখন লাইভ! 🚀

**লাইভ URL:** https://your-project.vercel.app

---

**Last Updated:** February 22, 2026  
**Created For:** Sajeb NEWS v2.0
